using System;

namespace Projeto56
{
    class Program
    {
        static void Main(string[] args)
        {
            /*Formatar:
             * ToLower()=>TudoMinusculo
             * ToUpper()=>TudoMinusculo
             * Trim()=>ApagaOsEspaçosAntesEDepoisDoString
             * IndexOf()=>ProcuraPrimeiraAparenciaDentroDoString(RetornaAPosicao)
             * LastIndexOf()=>ProcuraUltimaAparenciaDentroDoString
             * Substring(n)=>CortaAPartirDaPosiçao'n'
             * Substring(n, m)=>CortaAPartirDaPosiçao'n'EApenas'm'Caracteres
             * Replace('a', 'b')=>TrocaTodoCarectere'a'Por'b'(PossoTrocarMaisDe'1'Carectere)
             * bool b1 = String.IsNullOrEmpty(string)=>TesteParaVerSeEEspaçoVazioOuNulo
             * bool b2 = String.IsNullOrWhiteSpace(string)=>CasoEscrevaVariosEspaçosBrancos
             * 
             
        }
    }
}
